# Pin gtk-android-builder (Pixiewood) for reproducible Android CI builds.
# Bump this commit when intentionally upgrading the Android packaging toolchain.
# Used by scripts/android/build-pixiewood-apk.sh and the Android build workflow cache key.
00b1862b6434a8778557dc3d3074f43f5fdead1b
